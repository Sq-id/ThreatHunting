Reconissance Detection in Wireshark:
----------------------------------------
Ping Sweep detection:
  1. icmp.type == 8 || icmp.type == 0
     #ICMP Type 8 = ECHO Request
     #ICMP Type 0 = ECHO Reply
    NOTE: Most the time ICMP is blocked so this wont come up alot

ARP Sweep Detection (PyShark)
  2. add here
